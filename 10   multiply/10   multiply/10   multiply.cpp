# include < iostream > 
using namespace std;
void main()
{
	int x, y;
	cout << " Enter The Two Numbers \n ";
	cin >> x >> y;
	if (x%y == 0 || y%x == 0)
	
		cout << " Multiply \n ";
	
	else
		cout << " No Mutiply \n ";
}